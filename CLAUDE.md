# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is the **Coursera Expense Tracker AI** project. The repository is in its initial setup phase — no source code, build configuration, or dependencies have been committed yet.

## Getting Started

Once the project structure is established, this file should be updated with:

- Build, lint, and test commands (e.g., `npm run build`, `npm run lint`, `npm test`)
- How to run a single test (e.g., `npm test -- --testPathPattern=<filename>`)
- Environment variable setup instructions
- Any required external services or API keys (names only, not values)

## Architecture

Once the codebase is scaffolded, document the high-level architecture here, including:

- Framework and runtime (e.g., Next.js, Express, FastAPI)
- AI/LLM integration approach (e.g., Anthropic SDK, OpenAI SDK, LangChain)
- Data layer (e.g., database type, ORM)
- Key data flows (e.g., how expenses are submitted, categorized by AI, and stored)
