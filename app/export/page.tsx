import ExportHub from "@/components/export/ExportHub";

export const metadata = {
  title: "Export & Sync Hub — ExpenseTracker",
  description: "Export, share, and sync your expense data with cloud services",
};

export default function ExportPage() {
  return <ExportHub />;
}
